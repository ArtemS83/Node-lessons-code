const info = (msg) => {
  console.log(`Info: ${msg}`)
}

const log = (msg) => {
  console.log(`Log: ${msg} ${a}`)
}

module.exports = {
  info,
  log,
}

// module.exports.info = info
// module.exports.log = log
